_schema-version: "3.2"
ID: comtrialses
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comtrialses-destination-content
  type: com.sap.application.content
  requires:
  - name: comtrialses-destination-service
    parameters:
      content-target: true
  - name: comtrialses-repo-host
    parameters:
      service-key:
        name: comtrialses-repo-host-key
  - name: comtrialses-uaa
    parameters:
      service-key:
        name: comtrialses-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comtrialses_html_repo_host
          ServiceInstanceName: comtrialses-html5-service
          ServiceKeyName: comtrialses-repo-host-key
          sap.cloud.service: comtrialses
        - Authentication: OAuth2UserTokenExchange
          Name: comtrialses_uaa
          ServiceInstanceName: comtrialses-xsuaa-service
          ServiceKeyName: comtrialses-uaa-key
          sap.cloud.service: comtrialses
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comtrialses-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comtrialses-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comtrialses.zip
      name: comtrialses
      target-path: resources/
- name: comtrialses
  type: html5
  path: .
  build-parameters:
    ignore:
      "node_modules/"
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comtrialses-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comtrialses-destination-service
    service-plan: lite
- name: comtrialses-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comtrialses-xsuaa-service
    service-plan: application
- name: comtrialses-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comtrialses-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
